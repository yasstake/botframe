mod sqlite;

/*
use crate::common::time::NanoSec;
use crate::common::order::Trade;


// 実行ログを保存する。
// とりあえず毎回コミットでいいやっ。
pub trait ExecuteLog {
    fn insert(trade: Trade);
}

 */

/*
// OHLCVのデータを作成する。
//
pub struct Ohlcvv {
    start: NanoSec,
    end: NanoSec,
    open: f64,
    high: f64,
    low: f64,
    close: f64,
    total_volume: f64,
    sell_volume: f64,
    buy_volume: f64,
}
*/


/*
pub trait HistoricalData {
    fn execute_log(&self, start_time: &self) ; // -> impl Iterator<Item=Trade>; // iterator of Trade
    fn time_olhcvv(&self, end_time:f64, width_sec: u32, num_of_bars: u32); //  -> Iterator<Ohlcvv>;
    fn vol_olhcvv(&self, end_time:f64, width_vol: u32, num_of_bars: u32); //  -> Iterator<Ohlcvv>;
    fn vol_bars(&self, end_time:f64, width_vol: u32, num_of_bars: u32); //  -> Iterator<Ohlcvv>;
}
*/



